-- `set` contains vim options
require 'sami.set'
-- `remap` contains key mappings
require 'sami.remap'
-- `lazy` contains configuration & plugins for the Lazy package manager
require 'sami.lazy'
-- `autocmd` contains autocommands
require 'sami.autocmd'
